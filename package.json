{
  "scripts": {
    "start-db": "mongod --shutdown && mongod --fork --syslog",
    "start-apollo": "node ./apollo/start.js",
    "serve": "webpack-dev-server",
    "start-all": "nodemon --exec 'npm run start-db && npm run start-apollo && npm run serve'"
  },
  "devDependencies": {
    "html-webpack-plugin": "^3.2.0",
    "mongodb": "^3.2.7",
    "nodemon": "^1.19.1",
    "text-encoding": "^0.7.0",
    "webpack": "^4.29.3",
    "webpack-cli": "^3.2.3",
    "webpack-dev-server": "^3.1.14"
  },
  "dependencies": {
    "apollo-server": "^2.5.0",
    "graphql": "^14.3.0"
  }
}
